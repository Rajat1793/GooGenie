name: backend-checks

on:
  push:
    paths:
      - "backend/**"
      - ".github/workflows/backend-checks.yml"
  pull_request:
    paths:
      - "backend/**"
      - ".github/workflows/backend-checks.yml"

jobs:
  checks:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.1.3

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "pnpm"
          cache-dependency-path: "pnpm-lock.yaml"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm --filter googenie-backend run lint

      - name: Typecheck
        run: pnpm --filter googenie-backend run typecheck

      - name: Test
        run: pnpm --filter googenie-backend run test

      - name: Dependency audit (high and above)
        run: pnpm --filter googenie-backend run audit:deps
        continue-on-error: true
